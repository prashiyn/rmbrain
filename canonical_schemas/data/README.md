# Data folder

Seed data for canonical schemas lives inside the package at **`src/canonical_schemas/data/`** (e.g. `actors_seed.json`). Use `from canonical_schemas.data import load_seed_actors` to load it.

This folder is for any repo-level or non-packaged data if needed.
